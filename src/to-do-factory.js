// To do factory function.
const toDo = (title) => {

    const description = '';
    const dueDate = '';
    const priority = '';

    return {
        title,
        description,
        dueDate,
        priority,
    }
};

export default toDo;